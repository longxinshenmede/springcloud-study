server:
  port: 7002
eureka:
  instance:
    hostname: eureka7002.com #服务实例名称
  client:
    register-with-eureka: false #不注册自己
    fetch-registry: false #注册中心不需要去检索其他注册中心
    service-url:
      defaultZone: http://eureka7001.com/eureka/
